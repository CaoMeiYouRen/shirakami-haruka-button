name: Release
on:
  push:
    branches:
      - master
jobs:
  release:
    name: Release
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - uses: actions/setup-node@v1
        with:
          node-version: "14"
      - run: npm i
      - run: npm run lint
      - run: npm run build
      - env:
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
        run: npm run release
